ProcessSlot


定义四个方法,表明自己既是链条中的链子也是链条本身
entry (入口)
nextEntry (调用下一个入口)
exit (出口)
nextExit (下一个出口)




链条操作:
  入口
  出口
  添加链子(在头部添加,在尾部添加),为了提高性能,需要提供头节点(添加头部都是o(1)复杂度)和尾节点(添加尾部是o(1)时间复杂度)
  移除链子(o(n)复杂度)

链子操作:
  entry
  exit

  next 定义链子
  setNext()
  getNext()

